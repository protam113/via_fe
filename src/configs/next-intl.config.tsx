import type { IntlConfig } from 'next-intl';

const config: IntlConfig = {
  locale: 'en', // default locale (khi cần)
  // **KHÔNG** dùng 'locales' nhé, chỉ là 1 chuỗi defaultLocale thôi
};

export default config;
